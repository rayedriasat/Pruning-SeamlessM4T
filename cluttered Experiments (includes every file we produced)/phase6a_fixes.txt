PHASE 6A COMPLETE FIX - APPLY ALL 5 CHANGES
=============================================

CHANGE 1: CIF Threshold (2 locations)
--------------------------------------
Location 1: CIFConnector class definition
FIND:
    def __init__(self, d_model=1024, n_refiner_layers=2, n_langs=45, threshold=0.70):

REPLACE WITH:
    def __init__(self, d_model=1024, n_refiner_layers=2, n_langs=45, threshold=0.50):


Location 2: Installation in remove_text_decoder_and_install_cif()
FIND:
    mdl.cif_connector = CIFConnector(d_model=hidden, n_refiner_layers=2,
                                     n_langs=n_langs+5, threshold=0.70)

REPLACE WITH:
    mdl.cif_connector = CIFConnector(d_model=hidden, n_refiner_layers=2,
                                     n_langs=n_langs+5, threshold=0.50)


CHANGE 2: Loss Weights
-----------------------
FIND:
        loss = (0.50 * cos_loss +      # INCREASED from 0.40
                0.20 * mse_loss +
                0.20 * qty_loss +      # DECREASED from 0.30
                0.10 * spk_reg)

REPLACE WITH:
        loss = (0.30 * cos_loss +      # Direction alignment
                0.40 * mse_loss +      # Magnitude alignment (KEY FIX)
                0.25 * qty_loss +      # Quantity prediction
                0.05 * spk_reg)        # Speaker regularization


CHANGE 3: Learning Rate
------------------------
FIND:
optimizer_6a = torch.optim.AdamW([
    {'params': model_6a.cif_connector.parameters(),   'lr': 5e-4, 'weight_decay': 0.01},  # INCREASED from 2e-4
    {'params': model_6a.speaker_adapter.parameters(), 'lr': 2e-4, 'weight_decay': 0.01},  # INCREASED from 1e-4
], betas=(0.9, 0.98))

REPLACE WITH:
optimizer_6a = torch.optim.AdamW([
    {'params': model_6a.cif_connector.parameters(),   'lr': 3e-4, 'weight_decay': 0.01},  # REDUCED for stability
    {'params': model_6a.speaker_adapter.parameters(), 'lr': 1e-4, 'weight_decay': 0.01},  # Keep same
], betas=(0.9, 0.98))


CHANGE 4: Scheduler
-------------------
FIND:
scheduler_6a = torch.optim.lr_scheduler.CosineAnnealingWarmRestarts(
    optimizer_6a, T_0=1000, T_mult=2, eta_min=1e-5)

REPLACE WITH:
scheduler_6a = torch.optim.lr_scheduler.CosineAnnealingLR(
    optimizer_6a, T_max=5000, eta_min=1e-5)


CHANGE 5: Batch Accumulation
-----------------------------
FIND:
BATCH_ACCUM   = 2      # No accumulation needed with batch_size=8

REPLACE WITH:
BATCH_ACCUM   = 1      # Real batch size = 8


AFTER APPLYING ALL FIXES:
==========================
1. Delete checkpoints: !rm -rf checkpoints/phase6a*.pt
2. Restart training from step 0
3. Expected at step 500: cosine ~0.20-0.25 (vs current 0.42)
4. Expected at step 1500: cosine ~0.10-0.12
5. Expected at step 3000: cosine ~0.05-0.07

KEY INSIGHT:
The MSE weight increase (0.20 → 0.40) is the CRITICAL fix.
Teacher embeddings have both direction AND magnitude.
Cosine only matches direction, MSE matches magnitude.
Without strong MSE, cosine gets stuck at ~0.40.
